{
  "name": "sauron",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "update:submodules": "git submodule update --init --recursive",
    "install:repo": "npm install",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "update-sdl": "npm run update-sdl",
    "start-federation": "npm run start-federation",
    "start": "turbo run federation"
  },
  "devDependencies": {
    "turbo": "^2.0.12"
  }
}
